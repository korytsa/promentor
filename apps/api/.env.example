PORT=3000

# PostgreSQL
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/promentor?schema=public"

# Access JWT (short-lived) — default 900s = 15 minutes
JWT_SECRET="replace-with-a-strong-secret"
JWT_ACCESS_EXPIRES_IN_SECONDS=900
JWT_REFRESH_EXPIRES_DAYS=7

# CORS: frontend origin(s), comma-separated. Required for cookie auth across origins.
CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# Cookie SameSite: lax (same-site dev) | none (cross-site SPA + API, needs HTTPS in prod)
COOKIE_SAME_SITE=lax

# Frontend origin for OAuth callback redirects
FRONTEND_URL=http://localhost:5173

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback

# Optional hardening for signed OAuth state (falls back to JWT_SECRET if omitted)
GOOGLE_STATE_SECRET=

# Security policy: keep disabled unless mentor onboarding is controlled
ALLOW_GOOGLE_MENTOR_SIGNUP=false